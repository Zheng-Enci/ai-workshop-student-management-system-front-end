# AttendanceAnalysisPage - 时间线切换功能开发经验

## 功能概述
在签到分析页面实现签到时间线切换功能，支持月、年、总、周的时间线切换，参考数据看板页面的排行榜切换方式。

## 技术实现

### 1. 切换组件设计
参考数据看板页面的排行榜切换方式，使用 `el-select` 组件实现时间范围选择：

```javascript
const timeRangeOptions = [
  { label: '今日', value: 'today' },
  { label: '本周', value: 'week' },
  { label: '本月', value: 'month' },
  { label: '本年度', value: 'year' },
  { label: '全部', value: 'all' },
  { label: '自定义', value: 'custom' }
]
```

### 2. 时间范围计算
根据不同选择计算对应的时间范围：

```javascript
const loadTimelineData = async () => {
  let startTime, endTime
  
  switch (selectedTimeRange.value) {
    case 'today': {
      const today = new Date()
      startTime = new Date(today.getFullYear(), today.getMonth(), today.getDate(), 0, 0, 0)
      endTime = new Date(today.getFullYear(), today.getMonth(), today.getDate(), 23, 59, 59)
      break
    }
    case 'week': {
      const now = new Date()
      const dayOfWeek = now.getDay()
      const diff = now.getDate() - dayOfWeek + (dayOfWeek === 0 ? -6 : 1)
      const monday = new Date(now.setDate(diff))
      startTime = new Date(monday.getFullYear(), monday.getMonth(), monday.getDate(), 0, 0, 0)
      endTime = new Date()
      break
    }
    case 'month': {
      const now = new Date()
      startTime = new Date(now.getFullYear(), now.getMonth(), 1, 0, 0, 0)
      endTime = new Date()
      break
    }
    case 'year': {
      const now = new Date()
      startTime = new Date(now.getFullYear(), 0, 1, 0, 0, 0)
      endTime = new Date()
      break
    }
    case 'all': {
      startTime = PROJECT_LAUNCH_DATE
      endTime = new Date()
      break
    }
    case 'custom': {
      if (customDateRange.value && customDateRange.value.length === 2) {
        startTime = new Date(customDateRange.value[0])
        endTime = new Date(customDateRange.value[1])
        endTime.setHours(23, 59, 59, 999)
      } else {
        return
      }
      break
    }
  }
}
```

### 3. ECharts图表配置
根据时间范围类型显示不同的图表：

#### 今日时间线（散点图）
```javascript
if (selectedTimeRange.value === 'today') {
  // 显示今日签到时间分布散点图
  const option = {
    series: [
      {
        name: '签到时间线',
        type: 'scatter',
        data: timeData,
        itemStyle: {
          color: function(params) {
            const hour = params.data[0]
            if (hour >= 8 && hour < 12) return '#ff6b6b'
            if (hour >= 14 && hour < 18) return '#00d4ff'
            if (hour >= 19 && hour < 22) return '#ec4899'
            return '#8b5cf6'
          }
        }
      }
    ]
  }
}
```

#### 其他时间范围（折线图）
```javascript
else {
  // 显示每日签到人数折线图
  const option = {
    xAxis: {
      type: 'category',
      data: dates
    },
    series: [
      {
        name: '每日签到人数',
        type: 'line',
        data: counts,
        smooth: true,
        areaStyle: {
          color: {
            type: 'linear',
            colorStops: [
              { offset: 0, color: 'rgba(0, 212, 255, 0.4)' },
              { offset: 1, color: 'rgba(0, 212, 255, 0.1)' }
            ]
          }
        }
      }
    ]
  }
}
```

### 4. API接口集成
新增API接口函数：

```javascript
export const getDailyAttendanceCountInRange = async (startTime, endTime) => {
  try {
    const response = await api.get('/api/v1/attendance/daily-count-in-range', {
      params: {
        startTime,
        endTime
      }
    })
    return response.data
  } catch (error) {
    // 错误处理
  }
}
```

### 5. 样式设计
时间线控制器样式：

```css
.timeline-controls {
  display: flex;
  align-items: center;
  gap: 15px;
  margin-bottom: 20px;
  padding: 15px;
  background: var(--bg-secondary);
  border-radius: 12px;
  border: 1px solid var(--border-color);
}

.time-range-selector {
  flex: 1;
}

.custom-date-range {
  flex: 1;
}
```

## 关键要点

### 1. 用户体验一致性
- 参考数据看板页面的切换方式，保持界面一致性
- 使用相同的组件和交互模式
- 支持自定义日期范围选择

### 2. 数据可视化
- 今日显示散点图，展示具体签到时间
- 其他时间范围显示折线图，展示每日签到人数趋势
- 支持主题切换，夜间模式适配

### 3. 性能优化
- 按需加载数据，避免不必要的请求
- 图表实例管理，避免内存泄漏
- 响应式设计，支持不同屏幕尺寸

### 4. 错误处理
- 网络请求错误处理
- 参数验证
- 用户友好的错误提示

## 最佳实践

### 1. 组件设计
- 使用Element Plus组件保持一致性
- 响应式数据管理
- 生命周期钩子正确使用

### 2. 图表配置
- 动态主题适配
- 平滑的动画效果
- 清晰的视觉层次

### 3. 代码组织
- 功能模块化
- 错误处理完善
- 代码可维护性高

## 技术栈
- Vue3 Composition API
- Element Plus
- ECharts
- Pinia状态管理
- Axios网络请求

## 项目上线日期限制

### 问题
签到时间线不应显示早于项目上线时间（2025-9-9）的数据。

### 分析
需要设置项目上线日期常量，并在数据过滤和时间范围计算中使用。

### 方案
```javascript
const PROJECT_LAUNCH_DATE = new Date('2025-09-09T00:00:00')

const ensureTimeNotBeforeLaunch = (time) => {
  return time < PROJECT_LAUNCH_DATE ? PROJECT_LAUNCH_DATE : time
}

// 在时间线数据过滤中使用
const filteredData = data.filter(item => {
  const itemDate = new Date(item.date)
  return itemDate >= PROJECT_LAUNCH_DATE
})

// 在时间范围计算中使用
case 'all': {
  startTime = PROJECT_LAUNCH_DATE
  endTime = new Date()
  break
}
```

### 效果
- ✅ 时间线不显示早于2025-9-9的数据
- ✅ "全部"时间范围从项目上线日期开始
- ✅ 自定义时间范围自动限制最小日期
- ✅ 数据过滤确保历史数据不显示

### 要点
- 使用常量定义项目上线日期
- 在数据过滤和时间计算中统一使用
- 确保所有时间范围都受此限制

## 效果
- ✅ 支持多种时间范围切换
- ✅ 图表类型自动适配
- ✅ 主题切换支持
- ✅ 响应式设计
- ✅ 用户体验一致
- ✅ 项目上线日期限制
