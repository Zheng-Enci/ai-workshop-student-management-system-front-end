# DashboardPage - 时间段选项调整

## 问题描述

用户要求删除"本季度"和"本学期"选项，并增加一个"全部"选项。

## 实现方案

### 1. 修改时间段选项数组

**位置**：`src/views/DashboardPage/DashboardPageDesktop.vue` 第313-323行

**修改**：
```javascript
const timeRangeOptions = [
  { label: '今日', value: 'today' },
  { label: '本周', value: 'week' },
  { label: '本月', value: 'month' },
  { label: '本年度', value: 'year' },
  { label: '最近7天', value: 'last7days' },
  { label: '最近30天', value: 'last30days' },
  { label: '全部', value: 'all' },
  { label: '自定义', value: 'custom' }
]
```

**关键修改**：
- 删除 `{ label: '本季度', value: 'quarter' }`
- 删除 `{ label: '本学期', value: 'semester' }`
- 添加 `{ label: '全部', value: 'all' }`

### 2. 修改 Mobile 版本选项

**位置**：`src/views/DashboardPage/DashboardPageMobile.vue` 第70-77行

**修改**：
```vue
<el-radio-button label="today">今日</el-radio-button>
<el-radio-button label="week">本周</el-radio-button>
<el-radio-button label="month">本月</el-radio-button>
<el-radio-button label="year">本年度</el-radio-button>
<el-radio-button label="last7days">最近7天</el-radio-button>
<el-radio-button label="last30days">最近30天</el-radio-button>
<el-radio-button label="all">全部</el-radio-button>
<el-radio-button label="custom">自定义</el-radio-button>
```

### 3. 添加"全部"选项的处理逻辑

**位置**：`src/views/DashboardPage/DashboardPageDesktop.vue` 第466-473行

**添加**：
```javascript
case 'all': {
  const PROJECT_LAUNCH_DATE = new Date('2024-09-09T00:00:00')
  const now = new Date()
  const startTime = PROJECT_LAUNCH_DATE.toISOString().split('T')[0] + 'T00:00:00'
  const endTime = now.toISOString().split('T')[0] + 'T23:59:59'
  response = await getTopStudentsByTimeRange(startTime, endTime, selectedTopN.value)
  break
}
```

**说明**：
- 使用项目启动日期（2024-09-09）作为开始时间
- 使用当前日期作为结束时间
- 调用 `getTopStudentsByTimeRange` 获取全部数据

### 4. 移除不再使用的接口导入

**位置**：`src/views/DashboardPage/DashboardPageDesktop.vue` 第262-276行

**修改**：
```javascript
import {
  getMonthlyAttendanceCount,
  getDailyAttendanceCount,
  getCurrentMonthTop10Students,
  getDailyRanking,
  getWeeklyRanking,
  getMonthlyRanking,
  getYearlyRanking,
  getLast7DaysRanking,
  getLast30DaysRanking,
  getCustomRangeRanking,
  getTopStudentsByTimeRange,
  getVerificationCode
} from '@/api/attendance'
```

**移除**：
- `getQuarterlyRanking`
- `getSemesterRanking`

**添加**：
- `getTopStudentsByTimeRange`

### 5. 删除不再使用的 case 分支

**位置**：`src/views/DashboardPage/DashboardPageDesktop.vue` 第450-459行

**删除**：
```javascript
case 'quarter': {
  const currentQuarter = Math.ceil((new Date().getMonth() + 1) / 3)
  response = await getQuarterlyRanking(new Date().getFullYear(), currentQuarter, selectedTopN.value)
  break
}
case 'semester': {
  const currentSemester = new Date().getMonth() >= 1 && new Date().getMonth() <= 6 ? 1 : 2
  response = await getSemesterRanking(new Date().getFullYear(), currentSemester, selectedTopN.value)
  break
}
```

## 修复要点

### 1. 选项调整
- **删除选项**：移除"本季度"和"本学期"选项
- **新增选项**：添加"全部"选项，显示所有历史数据

### 2. 数据处理
- **时间范围**：使用项目启动日期到当前日期作为时间范围
- **接口调用**：使用 `getTopStudentsByTimeRange` 获取全部数据

### 3. 代码清理
- **移除导入**：删除不再使用的接口导入
- **删除分支**：移除不再使用的 case 分支

## 符合技术指令要求

✅ **按需导入**：使用按需导入方式  
✅ **代码规范**：符合 ESLint 规范  
✅ **功能完整**：Desktop 和 Mobile 版本都完成修改  
✅ **代码清理**：移除了不再使用的代码  

## 相关文件

- `src/views/DashboardPage/DashboardPageDesktop.vue`
- `src/views/DashboardPage/DashboardPageMobile.vue`





















