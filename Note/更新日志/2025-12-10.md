# 项目更新日志

## 2025年更新记录

### 2025-12-10

- 配置更新：创建更新日志管理机制，在 Note 文件夹下新增"更新日志"文件夹，用于按日期记录项目更新内容
- 配置更新：更新身份文档，添加更新日志编写规则和技术要求，包括文件管理规则、内容编写原则、格式要求、更新类型分类和编写时机等规范
- 配置更新：简化更新日志编写规则，精简为4条核心要点，提高可读性和执行效率
- 代码重构：将身份文档重构为文件夹结构，在 Note 文件夹下创建"本项目必须遵循的要求"文件夹，将原文档内容按功能分类为10个独立文档，包括身份声明、基础技术指令、第三方库使用规则、页面开发规则、特殊页面规则、文档管理规则、多端适配规则、文件存放规则、强制性行为准则和工作指引，提高文档的可维护性和查找效率
- 版本控制：提交所有变更到 Git 仓库，提交信息为"重构：将身份文档分类为多个文档，创建更新日志管理机制"，共修改16个文件，新增210行，删除118行
- 界面优化：优化工作指引文档，将内容改为条目式格式，去除冗余表述，增加更新日志记录要求，提高指引的清晰度和可执行性
- 版本控制：提交工作指引优化变更到 Git 仓库，提交信息为"优化：改进工作指引文档格式和内容"，共修改2个文件，新增7行，删除5行
- 配置更新：为"本项目必须遵循的要求"文件夹下的所有文档添加编号前缀，按照逻辑顺序编号为01-10，包括01-身份声明、02-基础技术指令、03-第三方库使用规则、04-页面开发规则、05-特殊页面规则、06-多端适配规则、07-文件存放规则、08-文档管理规则、09-强制性行为准则、10-工作指引，提高文档的查找和阅读顺序
- 版本控制：提交文档编号更新到 Git 仓库，提交信息为"配置更新：为项目要求文档添加编号前缀，优化文档顺序"，共重命名10个文件，修改2个文件，新增3行，删除1行
- 代码清理：清理工作指引文件中的测试内容，移除冗余文本
- 版本控制：提交清理变更到 Git 仓库，提交信息为"清理：移除工作指引文件中的测试内容"，共修改2个文件，新增1行，删除1行
- 配置更新：删除工作指引中的联网学习要求，简化工作指引内容
- 配置更新：恢复工作指引文件中的"工作："标识行
- 样式补齐：为超级管理员页面按需补充 Element Plus 组件样式导入（dialog/form/form-item/pagination/tooltip/input-number/date-picker/button-group），确保考勤记录等弹窗组件样式正常加载
- 文档优化：润色工作指引，明确起始、交互、规范、记录和经验补充的步骤
- 文档优化：工作指引强调需依据当前工作内容查阅对应经验文档（@YourExperience）
- 文档优化：将工作指引第5条调整为“收尾时，将本次工作的有用经验补充到（@YourExperience）”
- 样式补齐：补充 el-overlay 样式按需引入，修复考勤记录弹窗遮罩与居中显示
- 样式修复：新增 base.css 引入并为弹窗遮罩/容器设置固定定位和居中对齐，解决按需引入后弹窗出现在页面底部的问题
- 经验补充：记录弹窗位置异常的原因与修复（按需样式、定位兜底）到 `Note/YourExperience/AdminPage/弹窗位置异常-按需样式补齐与定位修复.md`
- 功能修复：为管理员页所有弹窗显式设置 `append-to-body` 与 `teleported`，进一步避免传送挂载异常
- 样式回滚：删除强制覆盖 `.el-overlay` / `.el-dialog__wrapper` 的全局兜底样式，恢复 Element Plus 默认显示/隐藏，避免弹窗初始出现在页面底部
- 功能修复：移除 `showDateDetailsDialog` 弹窗的 `v-if` 与 `v-model` 叠用，仅保留 `v-model` 控制显示，避免初始渲染导致弹窗内容裸露
- 功能修复：为全部弹窗补充 `v-if` 配合 `v-model` 控制，关闭时不渲染 DOM，防止初始内容裸露
- 样式兜底：为考勤记录/热力图/趋势图弹窗设置 `modal-class`，并在 scoped 样式用 `:deep` 固定 overlay/wrapper 定位与半透明背景，防止外部样式覆盖导致弹窗落底或透明
- 样式兜底修正：`modal-class` 的 overlay 不带 scope，需要在顶层使用 `:deep(.xxx-overlay)`/`:deep(.xxx-overlay .el-overlay-dialog)` 选择器，避免作用域嵌套导致样式不生效
- 功能修复：为管理员页所有弹窗显式设置 `append-to-body`，避免局部挂载导致对话框落在页面底部
- 功能更新：考勤记录改用 FullCalendar（月视图）展示，按需引入 @fullcalendar/core/daygrid/interaction/vue3 及 daygrid 样式，并以 `modal-class` 配合 scoped :deep 保证遮罩与居中
- 构建修复：将 FullCalendar daygrid 样式引入路径调整为 index.css，匹配新版包的 exports 设置，消除打包报错
- 代码清理：移除未使用的 isTimeSlotSigned 变量，修复 AdminConsolePage 的 ESLint no-unused-vars 报错
- 构建修复：因 @fullcalendar/daygrid 未导出 css，改为在页面样式内直接 `@import url('https://unpkg.com/@fullcalendar/daygrid@6.1.19/index.css');`，避免新增本地样式文件
- 构建修复：为 webpack 增加别名映射 `@fullcalendar/core/internal(.js)` 指向 node_modules 内部文件，解决运行时报错“Cannot find module '@fullcalendar/core/internal.js'”
- 界面调整：考勤记录日历由 FullCalendar 换用 Element Plus `el-calendar` 实现，移除 FullCalendar 依赖引用，使用自定义 date-cell 展示早/午/晚签到状态并点击查看当日详情
- 代码修复：修复 `isSlotSigned` 函数中调用不存在的 `getTimePeriod` 函数，改为使用 `getTimePeriodClass`，消除 ESLint no-undef 报错
- 组件导入修复：补充缺失的 Element Plus 组件导入（ElTooltip、ElPagination、ElFormItem、ElInputNumber、ElForm、ElDialog、ElCalendar、ElDatePicker）及 el-calendar 样式导入，解决运行时组件解析失败错误
- 组件导入修复：在 AdminPage.vue 中补充 ElIcon 组件导入及 el-icon 样式导入，解决运行时 "Failed to resolve component: el-icon" 错误
- 界面中文化：为考勤记录日历和日期选择器添加 Element Plus 中文 locale 配置（zhCn），确保月份、年份、星期等显示为中文；为 `attendance-calendar-admin` 添加星期中文显示的 CSS 样式覆盖
- 界面中文化完善：为考勤记录日历添加自定义 header 模板，将默认的英文按钮（Previous Month、Today、Next Month）替换为中文按钮（上个月、今天、下个月），并实现相应的月份切换函数（prevMonth、nextMonth、goToday）和格式化日历标题函数（formatCalendarTitle）
- ECharts 组件导入修复：补充缺失的 `HeatmapChart` 和 `VisualMapComponent` 导入，解决热力图图表使用但未导入的警告错误
- 经验总结：记录按需导入常见问题排查方法，核心经验为：从全量导入改为按需导入后容易出现遗漏导入问题，关键排查方法是查看浏览器控制台的警告和错误信息，根据提示补充缺失的导入；已补充到 `Note/YourExperience/AdminPage/FullCalendar接入考勤记录.md` 文档中
- 界面美化：美化补卡日期选择器，添加日历图标前缀、优化边框样式（圆角、阴影、过渡效果）、改善焦点状态（边框颜色和阴影）、添加悬停效果，并完善暗色模式支持，提升用户体验
- 界面美化完善：美化日期选择器下拉面板（picker-panel），优化面板圆角、阴影和边框，美化时间选择头部和日期头部样式，改善日期表格单元格样式（圆角、悬停效果、今日高亮、选中状态），将星期显示中文化（Sun/Mon等改为日/一等），并完善暗色模式支持
- 功能优化：为补卡日期选择器添加更大的独立弹出面板，通过 `popper-class="makeup-date-picker-popper"` 设置自定义样式类，增大面板宽度（420px）、单元格尺寸（36px）、字体大小和间距，提升日期选择的用户体验
- 界面中文化完善：完善日期选择器的中文化显示，通过 JavaScript 动态设置时间选择器占位符为中文（"选择日期"、"选择时间"），确保所有文本（包括占位符、月份名称、星期等）全部显示为中文
- 界面中文化修复：修复超级管理员页面补卡功能中日期选择器月份名称显示为英文的问题，通过 JavaScript 动态替换月份名称（如 "December" 改为 "十二月"），使用 MutationObserver 监听日期选择器面板变化，确保月份切换时也能正确显示中文月份名称
- 界面中文化修复：修复超级管理员页面补卡功能中日期选择器星期显示为英文的问题，在全局样式（`.makeup-date-picker-popper`）中添加星期显示的中文化处理，使用 CSS `::after` 伪元素将英文星期（Sun/Mon等）替换为中文（日/一等），并设置 `font-size: 0` 隐藏原文本，确保星期显示为中文
- 代码修复：修复 AdminConsolePage.vue 中的 ESLint 错误，移除未使用的变量 `index` 和 `cnWeek`，简化星期名称处理逻辑，确保代码符合 ESLint 规范
- 样式优化：将补卡日期时间选择器面板改为不透明，在全局样式和 scoped 样式中明确设置背景色为 `#ffffff` 和 `opacity: 1`，确保日期选择器面板完全不透明，提升视觉效果
- 样式修复：深度修复补卡日期选择器透明度问题，参考 Dialog 背景透明修复经验，在全局样式中为 `.makeup-date-picker-popper` 容器及其所有关键子元素（`.el-picker-panel`、`.el-picker-panel__body`、`.el-date-picker__header`、`.el-date-picker__time-header`、`.el-date-table`）明确设置 `background-color: #ffffff` 和 `opacity: 1`，确保日期选择器面板完全不透明
- 样式修复：修复补卡日期选择器暗色模式背景色问题，在全局样式中为暗色模式下的所有关键元素（容器、面板、头部、时间头部、日期表格）设置 `background-color: #000000` 和 `opacity: 1`，确保暗色模式下使用黑色背景且完全不透明
- 界面美化：美化补卡功能的时间选择器样式，优化时间选择头部布局（居中、间距），美化时间输入框样式（圆角、边框、阴影、过渡效果），改善焦点状态和悬停效果，完善暗色模式支持，提升时间选择的用户体验
- 布局优化：优化补卡功能时间选择器布局，将时间选择头部改为两列布局（左边日期，右边时间），使用 `flex: 1` 和 `max-width: 50%` 确保两列等宽显示，添加适当的间距，提升时间选择的视觉效果
- 功能增强：为超级管理员页面补卡功能添加快捷时间按钮组，提供6个常用时间段快捷选择（今天08:00/14:00/19:00、昨天08:00/14:00/19:00），点击按钮即可快速选择时间，提升补卡操作效率；快捷按钮支持选中状态高亮显示，与日期时间选择器联动，用户也可通过日期时间选择器自定义选择任意时间
- 界面美化：美化快捷时间按钮组样式，使用卡片式布局、圆角按钮、渐变背景（选中状态）、悬停效果和过渡动画，完善暗色模式支持，与整体UI风格保持一致，提升视觉体验
- 代码修复：修复 CSS 编译错误，将 CSS 中的 `//` 注释改为标准 CSS 注释格式 `/* */`，解决 PostCSS 解析器报错 "Unexpected '/'" 的问题
- 代码修复：修复 CSS 编译错误，将 scoped 样式中的 SCSS 语法 `&:hover` 和 `&.class` 改为标准 CSS 语法 `.class:hover` 和 `.class.other-class`，解决 PostCSS 解析器报错 "Unexpected '/'" 的问题
- 界面优化：优化补卡功能时间选择面板布局，将时间选择滚轮（小时、分钟、秒）改为横向排列，使用 flex 布局使三个滚轮并排显示，优化滚轮样式（字体大小、内边距、选中状态），美化确认和取消按钮样式（圆角、渐变背景、悬停效果），完善暗色模式支持，提升时间选择的用户体验和视觉效果
- 功能修复：修复时间选择器小时无法选择的问题，优化滚轮包装器宽度（min-width: 70px, max-width: 90px），确保滚轮区域可以正常交互（pointer-events: auto, cursor: pointer），优化滚动区域样式（overflow设置），添加悬停效果，确保小时、分钟、秒都可以正常选择和滚动
- 功能修复：修复时间选择器小时滚轮不显示的问题，确保所有三个滚轮（小时、分钟、秒）都正常显示，优化flex布局设置（flex-shrink: 0, flex-grow: 1），设置时间选择面板最小宽度（min-width: 280px）和时间选择器最小宽度（min-width: 200px），确保三个滚轮有足够的空间并排显示，使用nth-of-type选择器和:first-child选择器确保所有滚轮都可见，添加强制显示样式防止滚轮被隐藏
- 功能修复：修复超级管理员页面补卡功能时间选择器小时无法选择的问题，增加小时滚轮宽度（min-width: 70px, max-width: 100px），为小时滚轮设置更高的z-index（z-index: 2）确保不被遮挡，为所有滚轮和滚动条添加pointer-events: auto确保可交互，增加时间选择面板和滚轮容器的最小宽度（min-width: 300px），增加滚轮间距（gap: 12px），特别针对第一个滚轮（小时）设置cursor: pointer和更高的z-index，确保小时、分钟、秒三个滚轮都可以正常选择和滚动
- 功能重构：重构超级管理员页面补卡功能的时间选择模式，将原来的日期时间选择器改为日期选择器+24个按钮选择小时的方式，移除快捷时间按钮组和日期时间选择器，新增24个按钮（0-23小时）供用户选择，分钟和秒固定为00:00，使用网格布局（grid-template-columns: repeat(6, 1fr)）将24个按钮排列为6列4行，优化时间选择体验，简化操作流程
- 功能调整：恢复超级管理员页面补卡功能的日期选择为原来的日期时间选择器样式（type="datetime"），保持原有的日期选择器外观和功能（包括月份名称中文化等），但通过CSS隐藏时间选择面板，小时部分仍使用24个按钮选择，从日期时间字符串中提取日期部分与选择的小时组合生成最终的补卡时间
- 布局优化：优化超级管理员页面补卡功能的时间选择布局，将日期选择器和24小时按钮组改为左右并排显示（日期选择器在左边，小时按钮在右边），使用flex布局（display: flex, gap: 20px）实现左右布局，日期选择器区域设置固定最小宽度（min-width: 280px），小时按钮组区域使用flex: 1自适应剩余空间，确保点击补卡后日期选择器和小时按钮同时出现，提升用户体验
- 功能增强：实现补卡对话框打开时自动打开日期选择器的下拉面板，为日期选择器添加ref引用（datePickerRef），在openMakeupDialog函数中使用setTimeout延迟100ms后自动触发输入框的focus()和click()事件，确保点击补卡后日期选择器的下拉面板和小时按钮同时出现，无需用户手动点击日期选择器，提升操作效率
- 代码修复：修复日期选择器ref访问错误（datePickerRef.value.$el?.querySelector is not a function），改为直接使用DOM查询方式（document.querySelector('.makeup-date-picker input')）来找到输入框并触发打开，避免依赖ref的内部结构，提高代码的可靠性和兼容性
- 界面优化：删除补卡功能日期选择器的占位符文本（placeholder="请选择日期"），删除setPlaceholderAndMonth函数中设置占位符的代码逻辑，由于日期选择器面板会自动打开，不再需要占位符提示，简化界面显示
- 布局修复：修复补卡功能中日期选择面板和24小时按钮组重叠的问题，增加对话框宽度（从500px增加到900px），为日期选择器面板设置最大宽度（max-width: 450px）和更高的z-index（z-index: 2001），为日期选择器区域设置最大宽度（max-width: 350px）和z-index（z-index: 1），为小时按钮组设置z-index（z-index: 0），确保日期选择面板和小时按钮组不重叠，提升用户体验
- 功能重构：重构补卡功能为分步骤流程，添加步骤状态管理（makeupStep: 'date' | 'hour'），第一步显示日期选择器（type="date"），选择日期后点击确认进入第二步，第二步显示已选择日期和24小时按钮组，选择小时后点击确认补卡完成补卡操作，优化用户体验，避免同时显示日期和时间选择造成的重叠问题
- 代码修复：修复formatSelectedDate函数重复定义错误，将日期详情对话框使用的formatSelectedDate函数重命名为formatDateForDisplay，避免与补卡功能的formatSelectedDate函数冲突，解决编译错误
- 功能修复：修复补卡功能日期选择器无法选择日期的问题，为日期表格单元格（.el-date-table-cell、.el-date-table-cell__text、.el-date-table td）添加pointer-events: auto和cursor: pointer样式，确保日期单元格可以正常点击选择，解决日期选择器无法交互的问题
- 深度修复：继续修复日期选择器无法选择的问题，为日期选择器面板的所有层级元素（.makeup-date-picker-popper、.el-picker-panel、.el-picker-panel__body、.el-date-table）添加pointer-events: auto、position: relative和z-index设置，确保整个日期选择器面板都可以正常交互，解决可能被其他元素遮挡或样式覆盖导致的无法点击问题
- 功能移除：删除补卡功能的所有实现代码，包括补卡对话框、状态变量（makeupDialogVisible、makeupFormRef、datePickerRef、makeupStep、makeupForm、makeupMonthObserver）、所有补卡相关函数（handleDateChange、formatSelectedDate、confirmDateStep、selectHour、isHourSelected、updateAttendanceTime、openMakeupDialog、cancelMakeup、confirmMakeup）、验证规则（makeupDateFormRules）以及所有补卡相关的样式（scoped样式和全局样式），只保留补卡按钮并设置为禁用状态（disabled），保留API导入（makeupAttendanceWithSpecialPassword）以备将来使用
- 代码清理：删除未使用的导入和变量，移除补卡功能相关的API导入（makeupAttendanceWithSpecialPassword），删除残留的补卡相关函数（handleDateChange、formatSelectedDate、confirmDateStep、selectHour、isHourSelected、updateAttendanceTime），确保代码整洁，符合ESLint规范，无冗余代码
- 代码修复：删除空的全局样式标签（`<style></style>`），修复文件结构，确保Vue单文件组件格式正确，解决可能的编译错误
- 组件导入修复：修复 StudentManagerPage.vue 中缺失的 Element Plus 组件导入，补充 `ElDialog`、`ElDatePicker`、`ElCalendar` 组件导入及对应的样式文件导入（`el-dialog.css`、`el-date-picker.css`、`el-calendar.css`），解决运行时 "Failed to resolve component: el-date-picker/el-dialog/el-calendar" 错误
- 属性类型修复：修复 StudentManagerPage.vue 中 `ElDialog` 组件的 `transition` 属性类型错误，移除补卡对话框和考勤记录对话框中的 `:transition="false"` 属性，解决运行时 "Invalid prop: type check failed for prop 'transition'. Expected String | Object, got Boolean with value false" 错误
- 缓存问题说明：如果修复后仍然出现 transition 属性类型错误，可能是浏览器缓存或构建缓存导致，需要清除浏览器缓存（Ctrl+Shift+Delete）或重新构建项目（npm run serve）来确保最新代码生效
- 弹窗显示修复：修复 StudentManagerPage.vue 中弹窗显示在页面背面的问题，补充 `base.css` 和 `el-overlay.css` 样式导入，为所有对话框添加 `:teleported="true"` 属性，为关键对话框（补卡、考勤记录、热力图、趋势图、签到详情）添加 `modal-class` 属性，并在 scoped 样式中使用 `:deep()` 为 overlay 设置固定定位（`position: fixed`、`inset: 0`）和 z-index（3000/3001），确保弹窗悬浮显示在页面之上
- 日期时间选择器样式修复：修复 StudentManagerPage.vue 中补卡功能的日期时间选择器样式缺失问题，补充 `el-date-picker-panel.css`（日期选择面板样式）、`el-time-picker.css`（时间选择器样式）、`el-scrollbar.css`（滚动条样式）导入，确保 datetime picker 的所有组件（日期面板、时间选择器、滚动条）样式正常显示
- ECharts 组件导入修复：修复 StudentManagerPage.vue 中热力图功能缺失 ECharts 组件导入的问题，补充 `HeatmapChart`（从 `echarts/charts` 导入）和 `VisualMapComponent`（从 `echarts/components` 导入），并在 `echarts.use()` 中注册，解决运行时 "[ECharts] Component visualMap is used but not imported" 和 "[ECharts] Series heatmap is used but not imported" 警告
- 热力图 tooltip 禁用：禁用 StudentManagerPage.vue 中热力图（对话框和页面中的热力图）的 tooltip 显示，将 `tooltip` 配置设置为 `{ show: false }`，鼠标移动上去不再显示任何提示信息
- 热力图数字颜色设置：将 StudentManagerPage.vue 中热力图（对话框和页面中的热力图）的数字颜色设置为纯黑色（`#000000`），确保数字在所有主题下都清晰可见

