# 更新日志 - 2025-12-15

## 变更记录

- 超级管理员页面手机端考勤记录月份选择功能：在超级管理员页面手机端（`AdminPageMobile.vue`）的考勤记录对话框中添加月份选择功能，允许用户按月份筛选查看考勤记录。实现内容：1）状态管理：添加`selectedMonth`状态变量（格式为`YYYY-MM`），用于存储当前选择的月份；在打开对话框时初始化`selectedMonth`为空字符串，加载数据后自动选择第一个有签到的月份（最新的月份）；在关闭对话框时清空`selectedMonth`。2）可用月份提取：创建`getAvailableMonths`计算属性，从`studentAttendanceRecords`中提取所有有签到的月份，使用`Set`去重，按时间倒序排列（最新的月份在前），格式化为`{value: 'YYYY-MM', label: 'YYYY年MM月'}`的对象数组。3）记录过滤：创建`filteredAttendanceRecords`计算属性，根据`selectedMonth`过滤考勤记录；当`selectedMonth`为空时返回所有记录，否则只返回匹配月份的记录（通过比较`YYYY-MM`格式的月份键值）。4）月份选择器UI：在学生信息头部下方添加月份选择器容器（`.month-selector-mobile`），使用`el-select`组件实现下拉选择；添加"全部"选项（值为空字符串）用于查看所有月份的记录；只显示学生有签到的月份（通过`v-if="getAvailableMonths.length > 0"`控制显示）；选择器样式适配移动端（`flex: 1`、`max-width: 200px`）。5）显示逻辑更新：将考勤记录列表的数据源从`studentAttendanceRecords`改为`filteredAttendanceRecords`，确保只显示过滤后的记录；更新空状态提示（当选择的月份没有记录时显示"该月份暂无考勤记录"，否则显示"暂无考勤记录"）。6）样式设计：添加月份选择器容器样式（毛玻璃背景、圆角、边框、间距）、标签样式（字体大小、颜色、不换行）、选择器样式（flex布局、最大宽度限制）。符合功能完整性：实现完整的月份筛选功能；符合用户体验优化：自动选择最新月份、只显示有签到的月份、操作便捷；符合移动端设计规范：适配手机屏幕、样式美观。

- 超级管理员页面手机端学生补卡功能实现：在超级管理员页面手机端（`AdminPageMobile.vue`）实现学生卡片的补卡功能，允许管理员为学生进行补卡操作。实现内容：1）组件和样式导入：从Element Plus导入`ElDatePicker`组件；导入日期选择器相关样式文件（`el-date-picker.css`、`el-date-picker-panel.css`、`el-scrollbar.css`）；导入中文语言包（`zhCn`）用于日期选择器中文显示；从`@/api/attendance`导入`makeupAttendanceWithSpecialPassword` API函数。2）状态管理：添加`makeupDialogVisible`（控制补卡对话框显示）、`makeupLoading`（控制提交加载状态）、`makeupSelectedStudent`（当前补卡的学生信息）、`makeupStep`（补卡步骤：'date'或'hour'）、`makeupFormRef`（表单引用）、`makeupForm`（补卡表单数据：`attendanceTime`、`selectedDate`、`selectedHour`）等状态变量。3）时间段配置：定义`timeSlots`数组，包含三个时间段（早上8-11点、下午14-17点、晚上19-22点）及其可选小时；定义`validHours`数组（所有有效小时列表）用于验证；定义`makeupDateFormRules`表单验证规则（日期必选）。4）核心功能函数：实现`openMakeupDialog(student)`函数（打开补卡对话框、初始化表单数据、设置步骤为'date'）；实现`cancelMakeup()`函数（关闭对话框、重置表单、清理数据）；实现`handleDateChange()`函数（日期选择后自动切换到时间选择步骤）；实现`selectHour(hour)`函数（选择补卡小时、更新完整时间）；实现`isHourSelected(hour)`函数（判断小时是否被选中）；实现`updateAttendanceTime()`函数（根据日期和小时生成完整的补卡时间字符串）；实现`validateAttendanceTime(dateStr, hour)`函数（验证补卡时间不能晚于当前时间）；实现`formatSelectedDate()`函数（格式化选中的日期显示）；实现`nextStep()`函数（从日期选择步骤进入时间选择步骤、表单验证）；实现`submitMakeup()`函数（提交补卡请求、使用特殊密码、刷新考勤统计、错误处理）。5）对话框模板：创建补卡对话框，包含学生信息展示区域（头像、姓名、学号、年级专业）；第一步日期选择步骤（使用`el-date-picker`组件，支持中文显示，格式为YYYY-MM-DD）；第二步时间选择步骤（显示已选择日期，按时间段分组显示小时按钮，每个时间段显示标签和对应的小时按钮，提示信息说明时间段限制）；操作按钮（取消按钮、下一步按钮、确认补卡按钮，根据步骤显示不同按钮，支持loading状态）。6）样式设计：添加对话框样式（圆角、毛玻璃效果、阴影、最大高度、滚动）；学生信息卡片样式（flex布局、头像、信息展示）；步骤内容样式（标题样式、表单样式、日期选择器样式）；时间选择样式（已选择日期显示、时间段分组、小时按钮组、分隔线、提示信息）；底部按钮样式（flex布局、按钮尺寸、间距）。符合功能完整性：实现完整的补卡功能流程；符合用户体验优化：分步骤操作、清晰的时间段选择、实时验证；符合移动端设计规范：适配手机屏幕、按钮大小合适、样式美观。

- 超级管理员页面手机端补卡功能优化：在超级管理员页面手机端（`AdminPageMobile.vue`）优化补卡功能的用户体验和显示效果。实现内容：1）防止手机输入法弹出：为日期选择器添加`@focus`事件处理函数`handleDatePickerFocus`，在获得焦点时设置输入框的`readonly`和`inputmode="none"`属性；在`openMakeupDialog`函数中使用`setTimeout`延迟设置输入框属性，确保对话框完全渲染后再设置；为日期选择器的输入框添加CSS样式（`user-select: none`、`cursor: pointer`），提升用户体验。2）日期选择器月份中文化：为日期选择器添加`popper-class="makeup-date-picker-popper-mobile"`属性，用于定位日期选择面板；定义`monthMap`月份映射表（英文月份到中文月份的映射，如January→一月）；实现`setChineseMonthMobile()`函数，查找日期选择面板中的月份标签（`.el-date-picker__header-label`）并动态替换为中文；在`openMakeupDialog`函数中使用多个`setTimeout`（100ms、200ms、500ms、800ms）延迟调用`setChineseMonthMobile()`，确保面板渲染后替换月份；使用`MutationObserver`监听日期选择面板的DOM变化（`childList`、`subtree`、`characterData`），在用户切换月份时自动替换为中文；在`cancelMakeup`函数中断开`MutationObserver`并清理资源，避免内存泄漏；为日期选择器面板添加`z-index`样式，确保正确显示层级。3）日期选择器星期中文化：定义`weekDayMap`星期映射表（英文星期到中文星期的映射，如Sun→日、Mon→一等）；实现`setChineseWeekDaysMobile()`函数，查找日期选择面板中的星期单元格（`.el-date-table th`）并直接替换`textContent`为中文；在`openMakeupDialog`函数中与月份替换一起调用星期替换函数，使用多个延迟确保正确替换；在`MutationObserver`回调中同时调用星期替换函数，确保切换月份时星期也正确显示为中文；简化替换逻辑，直接使用`cell.textContent = cnDay`替换，避免复杂的节点操作。符合用户体验优化：防止输入法干扰、月份和星期显示中文、自动更新；符合移动端设计规范：适配手机交互、样式美观；符合代码规范：资源清理、避免内存泄漏、代码简洁。

- 超级管理员页面手机端补卡快捷日期选择功能：在超级管理员页面手机端（`AdminPageMobile.vue`）的补卡功能中添加快捷日期选择功能，允许用户快速选择今天或昨天的不同时间段。实现内容：1）快捷选项配置：定义`datetimeShortcuts`数组，包含6个快捷选项（今天早上、今天下午、今天晚上、昨天早上、昨天下午、昨天晚上）；每个快捷选项包含`key`（唯一标识）、`label`（显示标签）、`dateOffset`（日期偏移量，0表示今天，-1表示昨天）、`timeSlot`（时间段标识：morning/afternoon/evening）、`defaultHour`（默认小时：早上8点、下午14点、晚上19点）。2）辅助函数：实现`getShortcutDate(shortcut)`函数，根据快捷选项的`dateOffset`计算对应的日期值（格式为YYYY-MM-DD）；实现`selectDatetimeShortcut(shortcut)`函数，选择快捷选项时设置日期（`makeupForm.selectedDate`）、设置默认小时（`makeupForm.selectedHour`）、调用`updateAttendanceTime()`更新完整时间、自动切换到时间选择步骤（`makeupStep = 'hour'`）；实现`isDatetimeShortcutSelected(shortcut)`函数，判断快捷选项是否被选中（检查日期是否匹配、时间段是否匹配）。3）分组显示：创建`yesterdayShortcuts`计算属性，筛选`dateOffset === -1`的快捷选项（昨天相关）；创建`todayShortcuts`计算属性，筛选`dateOffset === 0`的快捷选项（今天相关）；将快捷按钮分为两行显示，第一行显示昨天的三个按钮，第二行显示今天的三个按钮。4）UI组件：在日期选择步骤（`makeupStep === 'date'`）中，在步骤标题下方添加快捷按钮组（`.date-shortcuts-mobile`）；使用两个`shortcut-row-mobile`容器分别显示昨天和今天的按钮；每个容器内使用`v-for`循环渲染对应分组的快捷按钮，每个按钮显示图标（`Clock`）和文字标签；按钮根据选中状态显示不同样式（`primary`类型表示选中，`default`类型表示未选中）；按钮点击事件绑定`selectDatetimeShortcut`函数。5）样式设计：快捷按钮组使用垂直flex布局（`flex-direction: column`），两行之间间距为`12px`；每行使用水平flex布局（`display: flex`），每行显示3个按钮，按钮宽度平均分配（`flex: 1`、`min-width: 0`）；按钮尺寸优化：内边距`padding: 8px 6px`、字体大小`font-size: 12px`、图标大小`font-size: 14px`、按钮间距`gap: 6px`，使按钮更小更紧凑；移除行标签（"昨天"、"今天"），按钮标签本身已包含日期信息，无需额外标签。符合用户体验优化：快速选择常用日期和时间、减少操作步骤、提升补卡效率、布局紧凑；符合移动端设计规范：按钮大小合适、每行3个按钮、两行布局、交互友好；符合功能一致性：与桌面端功能保持一致。

- 超级管理员页面手机端补卡时间按钮尺寸优化：在超级管理员页面手机端（`AdminPageMobile.vue`）的补卡功能中优化时间选择按钮的尺寸，使按钮更小更紧凑。实现内容：1）按钮宽度调整：将时间按钮的最小宽度从`70px`调整为`60px`，最大宽度从`100px`调整为`80px`，使按钮宽度更小，更适合移动端显示。2）按钮内边距优化：为时间按钮添加`padding: 6px 8px`，减小按钮内部空间，使按钮更紧凑。3）字体大小调整：为时间按钮添加`font-size: 12px`，减小按钮文字大小，与按钮尺寸匹配。4）按钮间距优化：将按钮之间的间距从`gap: 8px`调整为`gap: 6px`，减小按钮之间的间距，使布局更紧凑。符合移动端设计规范：按钮尺寸合适、布局紧凑、视觉统一；符合用户体验优化：减少占用空间、提升操作效率、界面美观。

- 超级管理员页面手机端编辑对话框闪烁问题修复：在超级管理员页面手机端（`AdminPageMobile.vue`）修复编辑对话框关闭时的闪烁问题。实现内容：1）问题分析：参考`Note/YourExperience/PointsDashboardPage/积分排行榜实现经验.md`中的弹窗闪烁问题修复经验，闪烁的根本原因是Element Plus Dialog的遮罩层（overlay）关闭动画与数据清空时机冲突，导致遮罩层在关闭后短暂显示造成闪烁。2）添加modal-class：为编辑对话框添加`modal-class="edit-dialog-overlay-mobile"`属性，用于定位遮罩层DOM元素。3）添加关闭事件：为编辑对话框添加`@close="cancelEdit"`事件处理，确保点击关闭按钮时调用关闭函数。4）优化关闭逻辑：在`cancelEdit`函数中，先直接操作DOM隐藏遮罩层（通过`document.querySelector('.edit-dialog-overlay-mobile')`查找遮罩层，设置`display: none`、`visibility: hidden`、`opacity: 0`），然后设置`editDialogVisible.value = false`，最后使用`setTimeout`延迟清空表单数据（`editFormRef.value?.resetFields()`、`currentEditStudentId.value = ''`），确保弹窗完全关闭后再清空数据，避免v-for在关闭动画过程中重新渲染。5）禁用遮罩层过渡动画：为`.edit-dialog-overlay-mobile`和`.edit-dialog-overlay-mobile .el-overlay-dialog`添加CSS样式，设置`transition: none !important`和`animation: none !important`，禁用遮罩层的过渡动画，确保遮罩层立即隐藏，避免关闭动画导致的闪烁；同时设置固定定位（`position: fixed`、`inset: 0`）和z-index，确保遮罩层正确显示。符合用户体验优化：消除关闭时的闪烁现象、提升交互流畅度；符合代码规范：参考项目经验文档、使用最佳实践；符合问题解决规范：分析根本原因、应用成熟解决方案。

- 超级管理员页面手机端热力图和趋势图功能实现：在超级管理员页面手机端（`AdminPageMobile.vue`）实现学生签到热力图和趋势图查看功能，允许管理员通过可视化图表查看学生的签到情况。实现内容：1）ECharts依赖导入：从`echarts/core`导入`echarts`核心库；从`echarts/charts`导入`LineChart`和`HeatmapChart`图表类型；从`echarts/components`导入`TitleComponent`、`TooltipComponent`、`GridComponent`、`VisualMapComponent`组件；从`echarts/renderers`导入`CanvasRenderer`渲染器；使用`echarts.use()`注册所有组件和渲染器。2）状态管理：添加`heatmapDialogVisible`（控制热力图对话框显示）、`trendDialogVisible`（控制趋势图对话框显示）、`allStudentAttendanceRecords`（存储所有考勤记录用于图表数据生成）、`heatmapChart`（热力图容器ref）、`lineChart`（趋势图容器ref）、`heatmapInstance`（热力图实例ref）、`lineInstance`（趋势图实例ref）等状态变量。3）数据生成函数：实现`generateHeatmapData()`函数，从考勤记录中提取数据，按星期（周一至周日）和时间段（上午、下午、晚上）统计签到次数，返回格式为`[时间段索引, 星期索引, 签到次数]`的数组；实现`generateLineData()`函数，从考勤记录中按日期统计签到次数，计算累计签到次数，返回包含`dates`（日期数组）和`values`（累计签到次数数组）的对象。4）图表初始化函数：实现`initHeatmapChart()`函数，初始化热力图实例，配置热力图选项（坐标轴、视觉映射、工具提示、样式等），适配移动端尺寸（图表高度75%、字体大小14px、视觉映射高度150px），禁用tooltip，横轴显示时间段（早上、下午、晚上），纵轴显示星期（一、二、三、四、五、六、日），添加点击事件监听，点击单元格时显示该周几该时间段会来的概率；实现`initLineChart()`函数，初始化趋势图实例，配置折线图选项（时间轴、累计签到次数、区域填充、样式等），适配移动端尺寸（字体大小10-11px、符号大小5px、线宽2px）。5）对话框函数：实现`openHeatmapDialog(student)`函数（打开热力图对话框、加载考勤记录、初始化图表）；实现`closeHeatmapDialog()`函数（关闭对话框、清理图表实例、隐藏遮罩层）；实现`openTrendDialog(student)`函数（打开趋势图对话框、加载考勤记录、初始化图表）；实现`closeTrendDialog()`函数（关闭对话框、清理图表实例、隐藏遮罩层）。6）UI组件：在学生卡片操作区域添加"热力图"和"趋势图"按钮（`type="info"`、`TrendCharts`图标）；创建热力图对话框，包含学生信息头部（头像、姓名、学号、年级专业、总签到次数）、图表容器（热力图和概率显示区域）、关闭按钮；创建趋势图对话框，结构与热力图对话框相同，显示趋势图图表。7）样式设计：添加遮罩层样式（`.heatmap-overlay-mobile`、`.trend-overlay-mobile`），禁用过渡动画避免闪烁；添加对话框样式（圆角、毛玻璃效果、阴影、最大高度70vh、滚动）；添加图表容器样式（背景、边框、内边距、高度300px、最小高度300px）。符合功能完整性：实现完整的热力图和趋势图查看功能；符合用户体验优化：图表适配移动端、交互流畅、数据清晰；符合移动端设计规范：对话框尺寸合适、图表大小适配、样式美观；符合代码规范：资源清理、避免内存泄漏、图表实例正确销毁。

- 超级管理员页面手机端热力图功能优化：在超级管理员页面手机端（`AdminPageMobile.vue`）优化热力图功能的显示和交互体验。实现内容：1）热力图布局优化：调整grid配置（`height: 75%`、`top: 1%`、`left: 10%`、`right: 5%`、`bottom: 20%`、`containLabel: true`），增加图表宽度和高度；横轴（xAxis）位置设置为`top`，显示时间段（早上、下午、晚上）；纵轴（yAxis）位置设置为`left`，显示星期（一、二、三、四、五、六、日）；禁用tooltip（`tooltip: { show: false }`），避免点击时显示默认提示。2）概率显示功能：实现`calculateDayProbabilities`计算属性，计算每个星期几会来的概率（从第一条签到记录到今天，统计每个星期几出现的总次数和有签到的次数，概率=有签到的次数/总次数×100%）；在热力图右侧添加概率显示区域（`.daily-averages-mobile`），显示每个星期几的概率百分比；调整概率显示区域样式（宽度50px、高度300px、与热力图对齐、间距优化）。3）时间段概率计算：实现`calculateTimeSlotProbabilities`计算属性，计算每个星期几的每个时间段（早上、下午、晚上）会来的概率；在热力图初始化时添加点击事件监听，点击单元格时通过`ElMessage`显示该周几该时间段会来的概率（格式：`周一 早上 会来的概率: 75.5%`）。4）样式优化：移除图表标题"签到热力图"；调整学生信息头部高度（减小padding、gap、头像尺寸、字体大小）；调整图表容器样式（移除padding和margin，使布局更紧凑）；优化概率显示区域样式（与热力图对齐、间距调整）。符合用户体验优化：热力图显示更清晰、概率信息直观、交互便捷；符合移动端设计规范：布局紧凑、信息完整、样式美观；符合功能完整性：实现概率计算和显示功能。

- 超级管理员页面手机端热力图和趋势图功能实现：在超级管理员页面手机端（`AdminPageMobile.vue`）实现学生签到热力图和趋势图查看功能，允许管理员通过可视化图表查看学生的签到情况。实现内容：1）ECharts依赖导入：从`echarts/core`导入`echarts`核心库；从`echarts/charts`导入`LineChart`和`HeatmapChart`图表类型；从`echarts/components`导入`TitleComponent`、`TooltipComponent`、`GridComponent`、`VisualMapComponent`组件；从`echarts/renderers`导入`CanvasRenderer`渲染器；使用`echarts.use()`注册所有组件和渲染器。2）状态管理：添加`heatmapDialogVisible`（控制热力图对话框显示）、`trendDialogVisible`（控制趋势图对话框显示）、`allStudentAttendanceRecords`（存储所有考勤记录用于图表数据生成）、`heatmapChart`（热力图容器ref）、`lineChart`（趋势图容器ref）、`heatmapInstance`（热力图实例ref）、`lineInstance`（趋势图实例ref）等状态变量。3）数据生成函数：实现`generateHeatmapData()`函数，从考勤记录中提取数据，按星期（周一至周日）和时间段（上午、下午、晚上）统计签到次数，返回格式为`[星期索引, 时间段索引, 签到次数]`的数组；实现`generateLineData()`函数，从考勤记录中按日期统计签到次数，计算累计签到次数，返回包含`dates`（日期数组）和`values`（累计签到次数数组）的对象。4）图表初始化函数：实现`initHeatmapChart()`函数，初始化热力图实例，配置热力图选项（坐标轴、视觉映射、工具提示、样式等），适配移动端尺寸（图表高度65%、字体大小11px、视觉映射高度150px）；实现`initLineChart()`函数，初始化趋势图实例，配置折线图选项（时间轴、累计签到次数、区域填充、样式等），适配移动端尺寸（字体大小10-11px、符号大小5px、线宽2px）。5）对话框函数：实现`openHeatmapDialog(student)`函数（打开热力图对话框、加载考勤记录、初始化图表）；实现`closeHeatmapDialog()`函数（关闭对话框、清理图表实例、隐藏遮罩层）；实现`openTrendDialog(student)`函数（打开趋势图对话框、加载考勤记录、初始化图表）；实现`closeTrendDialog()`函数（关闭对话框、清理图表实例、隐藏遮罩层）。6）UI组件：在学生卡片操作区域添加"热力图"和"趋势图"按钮（`type="info"`、`TrendCharts`图标）；创建热力图对话框，包含学生信息头部（头像、姓名、学号、年级专业、总签到次数）、图表容器（`.chart-container-mobile`、`.chart-item-mobile`、`.chart-content-mobile`）、关闭按钮；创建趋势图对话框，结构与热力图对话框相同，显示趋势图图表。7）样式设计：添加遮罩层样式（`.heatmap-overlay-mobile`、`.trend-overlay-mobile`），禁用过渡动画避免闪烁；添加对话框样式（圆角、毛玻璃效果、阴影、最大高度70vh、滚动）；添加图表容器样式（背景、边框、内边距、高度300px、最小高度300px）；图表标题样式（字体大小16px、居中、加粗）。符合功能完整性：实现完整的热力图和趋势图查看功能；符合用户体验优化：图表适配移动端、交互流畅、数据清晰；符合移动端设计规范：对话框尺寸合适、图表大小适配、样式美观；符合代码规范：资源清理、避免内存泄漏、图表实例正确销毁。
