# 超级管理员页面重构

## 文件组织结构

```
AdminPage/
├── AdminPage.vue                 # 主入口组件
├── AdminPageDesktop.vue          # 电脑端页面
├── AdminPageMobile.vue           # 移动端页面
├── css/                          # 样式文件
│   ├── desktop/                  # 电脑端样式
│   │   ├── AdminPage-identify_authentication_page.css
│   │   ├── AdminPage-main_page_buttons_and_search.css
│   │   ├── AdminPage-main_page_header.css
│   │   └── AdminPage-main_page_student_cards.css
│   └── mobile/                   # 移动端样式
│       └── AdminPageMobile.css
├── forms/                        # 表单组件
│   └── desktop/                  # 电脑端表单
│       ├── ChangeAdminForm.vue           # 修改所属管理员表单
│       ├── ChangeLevelForm.vue           # 修改学生身份表单
│       ├── CreatePointsRecordForm.vue     # 创建积分记录表单
│       ├── MakeupAttendanceForm.vue      # 补卡表单
│       └── ViewAttendanceRecordsForm.vue # 查看考勤记录表单
└── ts/                           # 逻辑文件
    ├── AdminPage.ts              # 页面状态和逻辑
    ├── AdminPageConfig.ts        # 配置常量
    ├── AdminPageUtils.ts         # 工具函数
    └── types/                    # 类型定义
        ├── index.ts
        ├── LevelMap.ts
        ├── RawStudentData.ts
        └── StudentInfo.ts
```

## 重构内容

### 整体重构
- 删除原有超级管理员页面代码，重新设计页面结构
- 采用模块化表单组件，提升代码可维护性
- 分离电脑端和移动端，适配不同设备

### 多关键词搜索
- 实现支持多个关键词的搜索功能
- 支持按学号、姓名等多种字段组合搜索

### 功能实现

#### 查看考勤记录
- 集成 ViewAttendanceRecordsForm 组件
- 支持查看学生的完整考勤记录

#### 修改学生身份
- 集成 ChangeLevelForm 组件
- 支持调整学生的身份类型

#### 修改所属管理员
- 集成 ChangeAdminForm 组件
- 支持变更学生所属的管理员

#### 补卡功能
- 集成 MakeupAttendanceForm 组件
- 支持使用特殊密码为学生补卡
- 自动获取当前特殊密码
- 自动传入选中学生信息
- 提供6个时间快捷选项（昨天/今天的上午/下午/晚上）
- 时间格式为 YYYY-MM-DDTHH:mm:ss

#### 考勤热力图
- 新增 HeatmapChartForm.vue 组件
- 支持展示学生一周七天、三个时段（上午/下午/晚上）的签到热力图
- 使用 ECharts 热力图可视化展示签到次数分布
- 支持深色/浅色主题自动适配
- 提供加载中、无数据等状态展示
- 样式类名采用 HeatmapChartForm-* 前缀，符合表单组件命名规范
- 依赖学生管理页面的考勤服务模块，支持复用现有接口
- 通过 defineExpose 暴露 openHeatmapDialog 方法供父组件调用
- 可在 AdminPageDesktop.vue 中集成，点击热力图按钮调用

#### 考勤趋势图
- 新增 AttendanceTrendChartForm.vue 组件
- 支持展示学生签到累计趋势图
- 使用 ECharts 折线图可视化展示签到次数变化
- 支持深色/浅色主题自动适配
- 提供加载中、无数据等状态展示
- 样式类名采用 AttendanceTrendChartForm-* 前缀，符合表单组件命名规范
- 依赖学生管理页面的考勤服务模块，支持复用现有接口
- 通过 defineExpose 暴露 openTrendChartDialog 方法供父组件调用
- 可在 AdminPageDesktop.vue 中集成，点击趋势图按钮调用
- 横坐标自动补全从最早到最晚考勤日期的所有日期，无考勤记录的日期签到次数为0

#### 创建积分记录
- 新增 CreatePointsRecordForm.vue 组件
- 支持通过特殊密码为指定学生创建积分记录
- 改分分数：数字输入框，支持正负值（正数为加分，负数为扣分），范围 -999 到 999
- 改分理由：多行文本输入，最长500字符，显示字数统计
- 表单验证：分数不能为空，理由不能为空且不超过500字符
- API 集成：调用 SpecialPasswordApi.createPointsRecord 创建积分记录
- 加载状态：显示全局加载蒙版
- 错误处理：友好的错误提示信息
- 在 AdminPageDesktop.vue 中集成，点击"修改积分"按钮打开表单
- 遵循现有表单组件风格和代码规范
- 使用特殊密码通过 Header X-Special-Password 传递
