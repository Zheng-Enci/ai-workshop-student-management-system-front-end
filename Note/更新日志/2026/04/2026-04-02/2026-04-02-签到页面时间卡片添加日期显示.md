# 2026-04-02 签到页面时间卡片添加日期显示

## 修改内容

### 1. 更新 AttendancePageMobile.vue

**新增响应式变量**:
```javascript
const currentDate = ref('')
```

**新增日期格式化逻辑**:
```javascript
const year = now.getFullYear()
const month = (now.getMonth() + 1).toString().padStart(2, '0')
const day = now.getDate().toString().padStart(2, '0')
const dateStr = `${year}年${month}月${day}日`
currentDate.value = dateStr
```

**新增HTML结构**:
```html
<div class="attendance-mobile-time-card-date-mobile">
    <el-icon class="attendance-mobile-time-card-date-icon-mobile">
        <Calendar/>
    </el-icon>
    <span>{{ currentDate }}</span>
</div>
```

### 2. 更新 attendance-mobile-time-card.css

**新增日期展示样式**:
```css
.attendance-mobile-time-card-date-mobile {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    font-size: 13px;
    color: var(--text-secondary);
    font-weight: 500;
    margin-bottom: 4px;
}

.attendance-mobile-time-card-date-icon-mobile {
    color: var(--text-tertiary);
    font-size: 16px;
}
```

## 效果说明

- 时间卡片现在显示：日期 + 时间
- 日期格式：`YYYY年MM月DD日`
- 日期显示在时间上方，使用较小的字体和次要颜色
- 带有日历图标

## 验证

- ✅ 日期变量添加成功
- ✅ 日期格式化逻辑添加成功
- ✅ HTML结构添加成功
- ✅ CSS样式添加成功
- ✅ 代码提交成功

## 相关文件

- `src/views/AttendancePage/AttendancePageMobile.vue`
- `src/views/AttendancePage/css/mobile/attendance-mobile-time-card.css`
