# 2026-04-02 签到页面移动端状态卡片样式提取

## 修改内容

### 1. 提取状态卡片样式到独立文件

- **文件**: `src/views/AttendancePage/css/mobile/status-cards.css`
- **说明**: 将签到页面移动端的状态卡片样式（status-cards-mobile、status-card-mobile、card-icon-mobile等）从 AttendancePageMobile.vue 提取到独立的 CSS 文件中
- **优势**: 
  - 提高代码可维护性
  - 便于样式复用
  - 优化代码结构

### 2. 更新 AttendancePageMobile.vue

- **修改**: 添加 `@import './css/mobile/status-cards.css'` 导入语句
- **删除**: 移除内联的 94 行状态卡片样式代码
- **净变化**: -93 行代码

## 技术细节

### 提取的样式模块

1. **状态卡片组** (`.status-cards-mobile`)
   - 网格布局：3列等宽
   - 间距：8px

2. **状态卡片** (`.status-card-mobile`)
   - 玻璃态效果：毛玻璃背景 + 边框 + 阴影
   - 交互效果：hover 上移 + 阴影放大
   - 状态样式：active（当前时段）、signed（已签到）

3. **卡片图标** (`.card-icon-mobile`)
   - 固定尺寸：32x32px
   - 圆角背景：8px
   - 颜色：默认主色调，已签到时为绿色

4. **卡片内容** (`.card-content-mobile`)
   - 居中对齐
   - 标题、时间、状态文本样式

## 验证

- ✅ 代码提交成功
- ✅ Git 日志记录完整
- ✅ 样式功能保持一致

## 相关文件

- `src/views/AttendancePage/AttendancePageMobile.vue`
- `src/views/AttendancePage/css/mobile/status-cards.css`
