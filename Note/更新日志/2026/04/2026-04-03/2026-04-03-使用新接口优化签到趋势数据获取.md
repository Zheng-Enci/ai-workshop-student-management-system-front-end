# 2026-04-03 使用新接口优化签到趋势数据获取

## 优化内容
- 新增 `AttendanceApi.getDailySignInCount` 方法，调用 `/api/v1/attendance/daily-sign-in-count` 接口
- 重构 `getAttendanceTrendData` 函数，从循环调用改为一次性获取整个日期范围数据
- 移除了 `getAttendanceCountByDate` 辅助函数

## 性能提升
- 之前：需要循环调用多次接口（如206天 = 多次请求）
- 现在：只需一次请求即可获取完整数据
