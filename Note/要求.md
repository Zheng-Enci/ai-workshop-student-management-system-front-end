# 开发要求

## 基本规范

- **自动 Git 操作**：每次修改文档都要自动执行 `git add` 和 `git commit`（但不能执行 `git push`）
- **自动更新日志**：在 `f:\BaiduSyncdisk\ZhengEnCi\AiWorkShop\AiWorkshopStudentManagementSystem\front-end\Note\更新日志` 目录下自动更新
- **输出简洁**：所有输出内容要简洁明了
