module.exports = {
	// 每行最大字符数
	printWidth: 120,
	// 缩进方式：tab（与eslint配置保持一致）
	useTabs: true,
	// 缩进级别（当不使用tab时）
	tabWidth: 4,
	// 语句末尾是否加分号（与eslint配置保持一致）
	semi: false,
	// 是否使用单引号（与eslint配置保持一致）
	singleQuote: true,
	// 对象属性是否使用引号（与eslint配置保持一致）
	quoteProps: 'as-needed',
	// JSX中是否使用单引号
	jsxSingleQuote: false,
	// 尾随逗号
	trailingComma: 'none',
	// 对象大括号内空格
	bracketSpacing: true,
	// 箭头函数参数括号
	arrowParens: 'avoid',
	// 换行符
	endOfLine: 'lf',
	// Vue文件脚本和样式标签缩进
	vueIndentScriptAndStyle: false
}
